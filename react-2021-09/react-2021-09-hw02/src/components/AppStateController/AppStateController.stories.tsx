import React from 'react';

import { AppStateController } from './AppStateController';

export default {
    title: 'AppStateController',
};

export const normal = () => {
    return <AppStateController />;
};
